@page "/"
@inject NavigationManager NavigationManager

@code { // hier redirect je van deze pagina naar de login pagina.
    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo("/login");
    }
}